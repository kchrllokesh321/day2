name: run-script

on:
  push:
    branches:
      - main

jobs:
  run-my-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run my script
        run: bash ./script.sh
